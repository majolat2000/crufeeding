services:
  - type: web
    name: crufeeding-backend
    runtime: node
    rootDir: backend
    plan: free
    buildCommand: npm install && npx prisma generate && npm run build
    startCommand: npx prisma db push --accept-data-loss --schema=./prisma/schema.prisma; npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_EXPIRES_IN
        value: 7d
      - key: DATABASE_URL
        fromDatabase:
          name: crufeeding-db
          property: connectionString
      - key: NEXT_PUBLIC_API_URL
        sync: false

databases:
  - name: crufeeding-db
    plan: free
    databaseName: crufeeding
    user: crufeeding
